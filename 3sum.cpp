// this is the cpp program to find the target in the given array
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int size;
    cout<<"Enter the size of the array"<<endl;
    cin>>size;
    vector<int> nums;
    cout<<"Enter the element in the array"<<endl;
    for(int i=0;i<size;i++)
    {
        int value;
        cout<<"Enter the value to be stored in the array"<<endl;
        cin>>value;
        nums.push_back(value);
    }
    set<<vector<int>> st;
    

    return 0;
}